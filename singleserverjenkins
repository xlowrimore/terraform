provider "aws" {
  region = "us-west-1"
# access_key = "insert access key" #
# secret_key = "insert secret key" #
}

resource "aws_instance" "jenkins" {
  
  
ami = "ami-056ee704806822732"
  instance_type = "t2.micro"
  key_name = "Nancy"
  security_groups = ["Jenkins"]
  user_data = user_data = "${file("nameoffile.tpl")}"
  
tags = {
    Name = "jenkins"
  }
}
