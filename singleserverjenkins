provider "aws" {
  region = "us-west-1"
  shared_credentials_file="/.aws/credentials"
}

resource "aws_instance" "jenkins" {
  
  
ami = "ami-056ee704806822732"
  instance_type = "t2.micro"
  key_name = "Nancy"
  security_groups = ["Jenkins"]
  user_data = user_data = "${file("init.tpl")}"
  
tags = {
    Name = "jenkins"
  }
}
